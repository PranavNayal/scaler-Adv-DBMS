# scaler-Adv-DBMS

This repository contains lab implementations for Advanced Database Management Systems.

- `Index/` contains a B-tree index implementation with insert/search functionality.
- `storage_buffer/` contains a Clock Sweep buffer manager implementation.

Build instructions:

1. Open the desired folder in CMake.
2. Configure with C++17.
3. Build the generated target.
